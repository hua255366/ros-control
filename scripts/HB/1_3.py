# -*- coding: utf-8 -*-'''project:The initial point of task 1 is on the left  # 项目说明：任务1的起始点在左侧version:1-1  # 版本号：1.1author:jiayu  # 作者：jiayudate:2024.6.26  # 日期：2024年6月26日'''# 导入所需库import cv2  # 计算机视觉库，用于摄像头图像采集与颜色识别import numpy as np  # 数值计算库，用于处理图像数组（如HSV颜色范围）from pymycobot.mycobot import MyCobot  # MyCobot机械臂控制核心类from pymycobot.genre import Angle  # 机械臂角度控制相关常量（当前代码未直接使用）import time  # 时间库，用于控制动作延迟（确保机械臂/夹爪动作完成）import basic  # 自定义基础工具库（需用户自行实现，代码中未体现具体功能）from GrabParams_2 import grabParams  # 自定义抓取参数库，存储机械臂串口、波特率等配置import math  # 数学库（当前代码未直接使用）import rospy  # ROS（机器人操作系统）节点库，用于创建节点和发布话题from geometry_msgs.msg import Twist  # ROS消息类型，用于控制底盘移动（线速度、角速度）# 初始化ROS节点，节点名为"movement"，anonymous=True确保节点名唯一（避免冲突）rospy.init_node('movement', anonymous=True)# 创建ROS发布者，发布话题为"cmd_vel"（底盘速度控制话题），消息类型为Twist，队列大小1pub = rospy.Publisher('cmd_vel', Twist, queue_size=1)# 设置ROS发布频率为20Hz（即每秒发布20次速度指令，确保底盘响应流畅）rate = rospy.Rate(20)# 初始化Twist类型消息，用于存储底盘速度指令move_cmd = Twist()# 定义全局变量motion_num，用于计数机械臂完成的抓取次数global motion_numspeed_fast = 65speed_normal =50speed_low =30# 初始化机械臂：传入串口设备路径和波特率（从grabParams配置中读取）mc = MyCobot(grabParams.usb_dev, grabParams.baudrate)'''# 以下为注释掉的颜色HSV范围配置（不同场景下的颜色阈值备选）"yellow": [np.array([22, 93, 0]), np.array([45, 255, 255])],  # 黄色HSV范围1"red": [np.array([170, 120, 120]), np.array([180, 255, 255])],  # 红色HSV范围1"green": [np.array([35, 43, 35]), np.array([90, 255, 255])],  # 绿色HSV范围1"blue": [np.array([90,43,46]), np.array([110,255,255])],  # 蓝色HSV范围1"purple": [np.array([140, 150, 120]), np.array([160, 255, 255])],  # 紫色HSV范围1---"yellow": [np.array([11, 115, 70]), np.array([40, 255, 245])],  # 黄色HSV范围2"red": [np.array([0, 43, 46]), np.array([8, 255, 255])],  # 红色HSV范围2（低色相段）"green": [np.array([35, 43, 35]), np.array([77, 255, 255])],  # 绿色HSV范围2"blue": [np.array(110,43,46]), np.array([124,255,255])],  # 蓝色HSV范围2（原代码此处缺"["，需注意语法错误）"purple": [np.array([120, 43, 46]), np.array([200, 255, 255])],  # 紫色HSV范围2# 以下为注释掉的最终颜色HSV阈值（当前代码未使用，作为备选）lower_blue = np.array([90, 43, 46])upper_blue = np.array([110, 255, 255])lower_red = np.array([170, 120, 120])upper_red = np.array([180, 255, 255])lower_yellow = np.array([26, 43, 46])upper_yellow = np.array([34, 255, 255])lower_green = np.array([35, 43, 35])upper_green = np.array([90, 255, 255])lower_purple = np.array([125, 43, 46])upper_purple = np.array([155, 255, 255])'''# ====================== 最终使用的颜色HSV阈值（用于图像识别）======================lower_blue = np.array([100, 43, 46])    # 蓝色HSV下限upper_blue = np.array([124,255, 255])   # 蓝色HSV上限lower_red = np.array([170, 120, 120])   # 红色HSV下限（高色相段，避免与橙色混淆）upper_red = np.array([180, 255, 255])   # 红色HSV上限lower_yellow = np.array([22, 93, 0])    # 黄色HSV下限upper_yellow = np.array([34, 255, 255]) # 黄色HSV上限lower_green = np.array([35, 43, 35])    # 绿色HSV下限upper_green = np.array([90, 255, 255])  # 绿色HSV上限lower_purple = np.array([120, 43, 46])  # 紫色HSV下限upper_purple = np.array([200, 255, 255])# 紫色HSV上限lower_orange = np.array([11, 43, 46])   # 橙色HSV下限upper_orange = np.array([25, 255, 255]) # 橙色HSV上限# 初始化抓取次数计数器，用于控制任务循环次数（完成3次后退出）motion_num = 0# ====================== 主任务循环（重复执行抓取动作，直到完成3次）======================while True:    # 1. 机械臂初始化：设置LED为红色（表示忙碌状态），夹爪张开（准备抓取）    mc.set_color(0, 0, 255)  # 设置机械臂LED为红色（RGB：0,0,255）    mc.set_gripper_value(255, 30)  # 夹爪张开到最大（值255），速度30    time.sleep(3)  # 等待3秒，确保夹爪完全张开    # 2. 机械臂移动到"视觉识别位置"（预定义角度）    angles = [-87.8, -32.34, 21.53, 31.72, -6.24, -49.26]  # 识别位置的6轴角度    ##angles = [-90.7, -28.12, -4.74, 52.2, -1.75, -50.88]    mc.send_angles(angles, 30)  # 按角度移动机械臂，速度30    time.sleep(3)  # 等待3秒，确保机械臂到达指定位置    # 3. 初始化摄像头（设备索引2，需根据实际摄像头连接情况调整）    cap = cv2.VideoCapture(2)    # time.sleep(8)  # 注释：备用延迟，用于摄像头启动稳定（当前未启用）    # ====================== 视觉识别子循环（底盘移动+颜色检测）======================    while True:        # 控制底盘以0.02m/s的速度前进（寻找目标物体）        move_cmd.linear.x = 0.03  # 线速度x轴（前进）设为0.02m/s        move_cmd.linear.z = 0     # 线速度z轴（垂直方向，底盘无此自由度）设为0        pub.publish(move_cmd)     # 发布速度指令到ROS话题        # 读取摄像头帧：retval（是否成功读取），frame（图像帧数据）        retval, frame = cap.read()        # 初始化识别标志：0=未识别到目标，1=识别到目标        flag = 0        # 将图像从BGR格式（OpenCV默认）转换为HSV格式（更适合颜色分割）        hsv = cv2.cvtColor(frame, cv2.COLOR_BGR2HSV)        # 生成各颜色的掩码（mask）：仅保留HSV范围内的像素（白色为目标，黑色为背景）        mask_blue = cv2.inRange(hsv, lower_blue, upper_blue)        mask_red = cv2.inRange(hsv, lower_red, upper_red)        mask_green = cv2.inRange(hsv, lower_green, upper_green)        mask_yellow = cv2.inRange(hsv, lower_yellow, upper_yellow)        mask_purple = cv2.inRange(hsv, lower_purple, upper_purple)        mask_orange = cv2.inRange(hsv, lower_orange, upper_orange)        # 寻找各颜色掩码中的轮廓（contour：目标物体的边缘轮廓）        contours_blue, _ = cv2.findContours(mask_blue, cv2.RETR_TREE, cv2.CHAIN_APPROX_SIMPLE)        contours_red, _ = cv2.findContours(mask_red, cv2.RETR_TREE, cv2.CHAIN_APPROX_SIMPLE)        contours_yellow, _ = cv2.findContours(mask_yellow, cv2.RETR_TREE, cv2.CHAIN_APPROX_SIMPLE)        contours_green, _ = cv2.findContours(mask_green, cv2.RETR_TREE, cv2.CHAIN_APPROX_SIMPLE)        contours_purple, _ = cv2.findContours(mask_purple, cv2.RETR_TREE, cv2.CHAIN_APPROX_SIMPLE)        contours_orange, _ = cv2.findContours(mask_orange, cv2.RETR_TREE, cv2.CHAIN_APPROX_SIMPLE)        # ---------------------- 红色物体识别与判断 ----------------------        for contour_red in contours_red:            # 计算轮廓面积（过滤掉过小/过大的干扰区域）            area_red = cv2.contourArea(contour_red)            if area_red > 5000 and area_red < 15000:  # 面积在5000-15000像素之间（目标大小）                # 计算轮廓的外接矩形：x（左上角x坐标）、y（左上角y坐标）、w（宽度）、h（高度）                x_red, y_red, w_red, h_red = cv2.boundingRect(contour_red)                # 在原图上绘制绿色矩形框（标记目标），线宽2                cv2.rectangle(frame, (x_red, y_red), (x_red + w_red, y_red + h_red), (0, 255, 0), 2)                print("x_red:", x_red, "y_red:", y_red)  # 打印目标坐标（调试用）                # 判断目标是否在摄像头画面的"中心区域"（x:300-450，y:140-200，需根据分辨率调整）                if (x_red > 300 and x_red < 450) and (y_red > 140 and y_red < 200):                    flag = 1  # 识别到目标，设置标志为1        # ---------------------- 黄色物体识别与判断 ----------------------        for contour_yellow in contours_yellow:            area_yellow = cv2.contourArea(contour_yellow)            if area_yellow > 5000 and area_yellow < 15000:                x_yellow, y_yellow, w_yellow, h_yellow = cv2.boundingRect(contour_yellow)                cv2.rectangle(frame, (x_yellow, y_yellow), (x_yellow + w_yellow, y_yellow + h_yellow), (0, 255, 0), 2)                print("x_yellow:", x_yellow, "y_yellow:", y_yellow)                if (x_yellow > 300 and x_yellow < 450) and (y_yellow > 140 and y_yellow < 200):                    flag = 1        # ---------------------- 蓝色物体识别与判断 ----------------------        for contour_blue in contours_blue:            area_blue = cv2.contourArea(contour_blue)            if area_blue > 3000 and area_blue < 20000:  # 蓝色目标面积范围放宽（3000-20000）                x_blue, y_blue, w_blue, h_blue = cv2.boundingRect(contour_blue)                cv2.rectangle(frame, (x_blue, y_blue), (x_blue + w_blue, y_blue + h_blue), (0, 255, 0), 2)                print("x_blue:", x_blue, "y_blue:", y_blue)                # 蓝色目标y坐标范围更窄（190-195），可能是特定高度要求                if (x_blue > 300 and x_blue < 450) and (y_blue > 190 and y_blue < 195):                    flag = 1        # ---------------------- 绿色物体识别与判断 ----------------------        for contour_green in contours_green:            area_green = cv2.contourArea(contour_green)            if area_green > 5000 and area_green < 20000:                x_green, y_green, w_green, h_green = cv2.boundingRect(contour_green)                cv2.rectangle(frame, (x_green, y_green), (x_green + w_green, y_green + h_green), (0, 255, 0), 2)                print("x_green:", x_green, "y_green:", y_green)                if (x_green > 300 and x_green < 450) and (y_green > 170 and y_green < 200):                    flag = 1        # ---------------------- 紫色物体识别与判断 ----------------------        for contour_purple in contours_purple:            area_purple = cv2.contourArea(contour_purple)            if area_purple > 5000 and area_purple < 15000:                x_purple, y_purple, w_purple, h_purple = cv2.boundingRect(contour_purple)                cv2.rectangle(frame, (x_purple, y_purple), (x_purple + w_purple, y_purple + h_purple), (0, 255, 0), 2)                print("x_purple:", x_purple, "y_purple:", y_purple)                if (x_purple > 300 and x_purple < 450) and (y_purple > 140 and y_purple < 200):                    flag = 1        # ---------------------- 橙色物体识别与判断 ----------------------        for contour_orange in contours_orange:            area_orange = cv2.contourArea(contour_orange)            if area_orange > 5000 and area_orange < 15000:                x_orange, y_orange, w_orange, h_orange = cv2.boundingRect(contour_orange)                cv2.rectangle(frame, (x_orange, y_orange), (x_orange + w_orange, y_orange + h_orange), (0, 255, 0), 2)                print("x_orange:", x_orange, "y_orange:", y_orange)                # 橙色目标x坐标范围更窄（350-450），可能是特定位置要求                if (x_orange > 350 and x_orange < 450) and (y_orange > 140 and y_orange < 200):                    flag = 1        # 显示处理后的图像帧（实时查看识别结果）        cv2.imshow('frame', frame)        # 退出视觉子循环的条件：1.识别到目标（flag=1）；2.按下键盘"q"键（手动退出）        if flag == 1:            break        if cv2.waitKey(1) & 0xFF == ord('q'):            break    # 4. 识别到目标后：停止底盘移动，释放摄像头资源    move_cmd.linear.x = 0  # 底盘线速度设为0（停止前进）    move_cmd.linear.z = 0    pub.publish(move_cmd)  # 发布停止指令    cap.release()          # 释放摄像头设备    cv2.destroyAllWindows()# 关闭所有OpenCV窗口（避免内存泄漏）    # 5. 机械臂移动到"抓取准备位置1"（调整姿态，靠近目标）    # angles = [-90.08, -3.25, -51.67, 117.59, 7.55, -48.25]    ## angles = [-80.85, -24.08, -33.31, 66.09, -9.22, -49.92]    ###angles = [-92.81,-29.0,-23.2,53.34,2.81,-44.38]    angles = [-88.85, -9.22, -33.48, 33.57, -1.58, -47.28]    mc.send_angles(angles, speed_fast)  # 速度65（较快移动）    time.sleep(1.5)  # 等待2秒，确保到位    # 6. 机械臂移动到"抓取准备位置2"（微调姿态，对准目标）    angles = [-90.35,5,-65,68,5.97,-45.79]    # mc.send_angles(angles, speed_fast)  # 速度65    time.sleep(1)  # 等待1秒    # 7. 执行抓取动作：夹爪闭合（值35，根据目标大小调整），速度50    mc.set_gripper_value(35, speed_normal)    time.sleep(1.5)  # 等待1.5秒，确保夹爪夹紧目标    # 8. 抓取后：机械臂移动到"抬起位置"（避免目标碰撞周围物体）    ##angles = [-89.47,32.51,-116.8,99.22,4.21,-46.66]    angles = [-85.86, 43.5, -102.04, 52.91, -5.36, -45.7]    mc.send_angles(angles, speed_fast)    time.sleep(1)    # 9. 机械臂移动到"放置准备位置1"（注释：当前未启用，备用姿态）    angles = [-88.59, 39.81, -90.96, 57.21, -7.82, -44.03]    # mc.send_angles(angles, 65)  # 代码注释：未执行该动作    time.sleep(1)    # 10. 机械臂移动到"放置准备位置2"（注释：当前未启用，备用姿态）    angles = [-90, 0, 0, 0, 0, -45]    # mc.send_angles(angles, 65)  # 代码注释：未执行该动作    time.sleep(1)    # 11. 机械臂移动到"放置位置"（调整姿态，准备释放目标）    angles = [-90.26, 70, -135, 64.33, 5.27, -46.58]    # mc.send_angles(angles, speed_fast)    time.sleep(1)    # 12. 机械臂移动到"最终放置位置"（微调姿态，确保目标放置稳定）    ##angles = [-79.18,3.07,-114.43,109.24,-11.77,-46.31]    angles = [-84.05, 6.94, -109.16, 93.33, -8.87, -46.14]    mc.send_angles(angles, speed_fast)    time.sleep(1)    # 13. 执行放置动作：夹爪张开（值255），速度65（快速释放）    mc.set_gripper_value(255, speed_fast)    time.sleep(2)  # 等待2秒，确保夹爪完全张开    # 14. 放置后：机械臂移动到"复位过渡位置1"（避免碰撞已放置的目标）    ##angles =[-99.05, 33.92, -124.62, 92.81,-2.75, -48.88]    ### angles =[-93.51, 48.15, -135.61, 82.44, 2.9, -44.29]    angles = [-86.57, 25.04, -53.08, 8.96, -5.8, -44.12]    mc.send_angles(angles, speed_fast)    time.sleep(1)    # 15. 机械臂移动到"复位过渡位置2"（注释：当前未启用，备用姿态）    # angles = [-90, 25, -100, 110, 0, -45]    # mc.send_angles(angles, 65)    # time.sleep(1)    # 16. 机械臂移动到"初始待机位置"（准备下一次抓取循环）    ##angles = [-90.7, -28.12, -4.74, 52.2, -1.75, -50.88]    angles = [-92.72, 14.5, -46.93, 39.81, -2.37, -41.92]    mc.send_angles(angles, speed_fast)    time.sleep(1)    # 17. 任务完成一次：设置LED为绿色（表示空闲状态），抓取次数+1    mc.set_color(0, 255, 0)  # LED设为绿色（RGB：0,255,0）    motion_num += 1    # 退出主循环的条件：完成3次抓取任务    if motion_num == 3:        break# ====================== 所有任务完成后：机械臂最终复位 ======================# 1. 机械臂移动到"最终复位位置1"（注释：当前未启用，备用姿态）angles = [-90, 0, 0, 0, 0, -45]# mc.send_angles(angles, 65)time.sleep(1)# 2. 机械臂移动到"最终复位位置2"（任务结束后的停靠姿态）angles = [11, -23, -39, -29, -3, 58]mc.send_angles(angles, speed_normal)  # 速度50（平稳移动）time.sleep(3)  # 等待3秒，确保机械臂稳定停靠